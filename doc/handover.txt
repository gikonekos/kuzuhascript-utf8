================================================================================
  kuzuhascript-utf8 / kscrr1p9up 作業引継ぎ
  2026-08-04 作成
================================================================================

## リポジトリ
  https://github.com/gikonekos/kuzuhascript-utf8

## 本番環境
  Oracle VPS / Ubuntu / Apache2
  設置先: /var/www/html/cgi-bin/k/
  URL: https://gikonekos.com/cgi-bin/k/bbs.cgi
  アップロード先: /var/www/html/upload/k/
  アップロードURL: https://gikonekos.com/upload/k/

## 本日完了した作業 (2026-08-04)

  [1] kscrr1p9up 動態保存・実機起動
      - パーミッション設定（bbs.cgi: 755, sub/*.pl: 644, log/count/upload: 777）
      - count/count0.dat, count1.dat 作成
      - frame.upd 作成（初期設定のアップロードログ）
      - upload/ を cgi-bin/ 外（/var/www/html/upload/k/）に移動
        → cgi-bin/ 内ではApacheがPNGをCGIとして実行しようとするため

  [2] バグ修正（bbsup.pl）
      - select(FLOG) → select(ULOG)（致命的バグ: アップロード時に誤動作）
      - prtupform() 内 my $ptext = &pcode の重複宣言除去

  [3] 脆弱性対策（bbsup.pl）
      - getmultipart() に HTTP_HOST チェック追加
      - open() 2引数 → 3引数化（ULOG, FILE）

  [4] 脆弱性対策（bbs.cgi × 2, bbsup.pl）
      - javascript: スキームを全角化して無害化（Encode::decode/encode使用）
      - getmultipart() の &quot; エスケープ欠落を補完

  [5] その他
      - require 'sub/bbsadmin.pl' → require './sub/bbsadmin.pl'（./ 補完）
        kscrr1p9/bbs.cgi・kscrr1p9up/bbs.cgi 両方

## 残課題・次段階

  [未着手]
  - jconv 代替の実装（現状 no-op、投稿データの文字コード正規化なし）
  - bbslog.pl「jcode.pl使用」チェックボックスの扱い（死んだUI）
  - admin機能側の権限チェック等の脆弱性監査（未着手）
  - 2引数 open() の残存箇所（設定値由来のみで実害なしだが3引数化推奨）
    該当: bbs.cgi の $logfilename, $countfile, $cntfilename, $oldlogfilename

  [設定値のプレースホルダー化（検討）]
  - bbsup.pl の $upfiledir, $upfileurl はサイト固有値のためリポジトリ上は
    プレースホルダーにしておくのが望ましい
  - 現状はオリジナル値（strange.kurumi.ne.jp）のまま残っている

  [GitHubに未反映の本番設定]
  - $cgiurl, $bbshost, $adminpost, $upfiledir, $upfileurl は
    本番サーバー上で手動設定済みだがリポジトリには入れていない（意図的）

## ファイル構成メモ（本番サーバー）

  /var/www/html/cgi-bin/k/
  ├── bbs.cgi          (755)
  ├── bbs.log          (666)
  ├── bbs.upd          (666)
  ├── frame.upd        (666)  ← アップロードログ
  ├── count/           (777)
  │   ├── count0.dat   (666)
  │   └── count1.dat   (666)
  ├── log/             (777)  ← 過去ログ出力先
  └── sub/             
      ├── bbsadmin.pl  (644)
      ├── bbscust.pl   (644)
      ├── bbslog.pl    (644)
      ├── bbssrc.pl    (644)
      ├── bbstopic.pl  (644)
      └── bbsup.pl     (644)

  /var/www/html/upload/k/  (777)  ← cgi-bin/ 外に配置
================================================================================
